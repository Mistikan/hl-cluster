---
apiVersion: v1
kind: Service
metadata:
  name: telemt
spec:
  type: ClusterIP
  selector:
    app: telemt
  ports:
    - name: "https"
      protocol: TCP
      port: 443
      targetPort: 443
---
apiVersion: v1
kind: Service
metadata:
  name: "telemt-node-service"
spec:
  type: NodePort
  selector:
    app: telemt
  ports:
    - name: "https"
      port: 443
      targetPort: 443
      nodePort: 30443
      protocol: TCP
