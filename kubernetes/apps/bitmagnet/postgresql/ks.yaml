---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: postgresql
  namespace: bitmagnet
spec:
  interval: 1h
  path: ./kubernetes/apps/bitmagnet/postgresql/app
  # postBuild:
  #   substituteFrom:
  #     - name: cluster-secrets
  #       kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: bitmagnet
  wait: false
