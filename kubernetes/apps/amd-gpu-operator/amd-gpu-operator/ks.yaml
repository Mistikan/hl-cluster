---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: amd-gpu-operator
  namespace: amd-gpu-operator
spec:
  interval: 1h
  path: ./kubernetes/apps/amd-gpu-operator/amd-gpu-operator/app
  # postBuild:
  #   substituteFrom:
  #     - name: cluster-secrets
  #       kind: Secret
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: amd-gpu-operator
  wait: false
